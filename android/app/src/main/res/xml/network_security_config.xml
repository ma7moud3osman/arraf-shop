<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev-only: allow plain HTTP to the Laravel dev server running on the
         local Mac. Update this IP if your Mac's LAN address changes
         (run `ipconfig getifaddr en0` on macOS). All other traffic remains
         HTTPS-only under Android's default cleartext block. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.7</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
